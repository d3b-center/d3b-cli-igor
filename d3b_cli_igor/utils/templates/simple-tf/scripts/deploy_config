project="{{ project }}"
BUCKET="{{ account }}-{{ account_id }}-{{ region }}-{{ environment }}-secrets"
ACCOUNT="{{ account }}"
REGION="{{ region }}"
ACCOUNT_ID="{{ account_id }}"
ENVIRONMENT="{{ environment }}"
